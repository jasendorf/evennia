apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evennia-data
  namespace: evennia
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
